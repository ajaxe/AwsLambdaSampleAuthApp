{
  "name": "sampleauthentication",
  "version": "1.0.0",
  "description": "Simple application served via AWS Lamabda with static file hosting in S3 bucket. Contains login functionality where the custom authorizer generates & verifies JWT tokens. The data store is dynamodb. For testing we'll be using AWS SAM Local & DynamoDb Local. This app uses following AWS services:",
  "main": "dist/index.js",
  "types": "dist/index.js",
  "scripts": {
    "test": "mocha -r ts-node/register --timeout 10000 src/**/*.spec.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.5.4",
    "aws-sdk": "^2.282.1",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "serverless-offline": "^3.25.6",
    "serverless-plugin-typescript": "^1.1.5",
    "ts-node": "^7.0.0",
    "typescript": "^2.9.2"
  },
  "dependencies": {
    "@aws/dynamodb-data-mapper": "^0.7.2",
    "@aws/dynamodb-data-mapper-annotations": "^0.7.2",
    "uuid": "^3.3.2"
  }
}
